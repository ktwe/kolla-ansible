[
  {version, 1},
  {nodes, [
  {% for host in groups[role_rabbitmq_groups] %}
    {'rabbit@{{ lookup('dig', hostvars[host]['ansible_facts'][api_interface].ipv4.address + '/PTR') }}', disc}
      {%- if not loop.last -%},{%- endif %}
  {% endfor %}
  ]},
  {gospel,
    {node, 'rabbit@{{ lookup('dig', hostvars[groups[role_rabbitmq_groups][0]]['ansible_facts'][api_interface].ipv4.address + '/PTR') }}'}}
].
